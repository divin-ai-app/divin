import { describe, expect, it } from "vitest";

// Placeholder so `npm run test` has something to run in CI until real unit
// tests land alongside Phase 2's validation schemas / schema.org builders.
describe("project scaffolding", () => {
  it("runs", () => {
    expect(1 + 1).toBe(2);
  });
});
