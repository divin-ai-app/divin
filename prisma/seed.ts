// Seed script placeholder — filled in during Phase 2 ("Data model + seeded
// profiles + profile pages") with ~20 Mauritius mock BusinessProfile rows
// across the 5 industries, plus CountryClearance and admin/staff bootstrap
// rows. See plan §7, Phase 2.
async function main() {
  console.log("No seed data yet — this lands in Phase 2.");
}

main();
